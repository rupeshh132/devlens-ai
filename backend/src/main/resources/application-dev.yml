spring:
  datasource:
    url: jdbc:h2:mem:devlensdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2-console
  flyway:
    enabled: false

  security:
    oauth2:
      client:
        registration:
          github:
            client-id: Ov23linU5RedVyvEUzjm
            client-secret: a7623860e3afe64d90f2ee27e68be09330dfd43d

app:
  oauth2:
    redirect-uri: "http://localhost:5173/oauth/callback"

gemini:
  api:
    url: "https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent"
    key: "YOUR_GEMINI_API_KEY_HERE"
